---
label: Block - Section
hide_body: true
fields:
- name: rows
  type: field_group_list
  fields:
  - name: row_name
    type: text
    config:
      required: false
    label: Row Name
    description: For Internal Use Only
  - name: columns
    type: field_group_list
    fields:
    - name: column_name
      type: text
      config:
        required: false
      label: Column Name
    - name: components
      type: blocks
      label: Components
      template_types:
      - component-text-block
      - component-image
      - component-button
      - spacer
      config:
        min: 
        max: 
    config:
      min: 
      max: 
      labelField: column_name
    label: Columns
  config:
    min: 
    max: 
    labelField: row_name
  label: Rows
